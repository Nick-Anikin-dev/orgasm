import { StyleSheet } from "react-native";

export const HomeClasses = StyleSheet.create(({
    welcome: {
        color: '#ffffff',
        fontSize: 36,
        lineHeight: 40,
        fontFamily: "Poppins_600SemiBold"
    }
}))
