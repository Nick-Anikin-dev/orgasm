import { StyleSheet } from "react-native";

export const EducationPartClasses = StyleSheet.create({
    scrollView: {
        backgroundColor: '#1F222A',
        minHeight: '60%',
        maxHeight: '74%',
        borderRadius: 12,
        marginTop: 10
    }
})
