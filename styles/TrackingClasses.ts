import { StyleSheet } from "react-native";

export const TrackingClasses = StyleSheet.create({
    label: {
        color: "#3A3D46",
        fontFamily: "Inter_700Bold",
        marginBottom: 10,
        fontSize: 11
    }
})
